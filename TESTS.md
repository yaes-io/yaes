## Testing

Every public function must have one test for the happy path and one test for each edge case. Use descriptive test names that explain the expected behavior.

## Build and Test

Use SBT with `-batch --no-colors` flags for tests. Prefer testing a single class or module for faster feedback.